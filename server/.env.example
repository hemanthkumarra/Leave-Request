# MongoDB Connection URI (recreate/change password if exposed)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.1cs3zy4.mongodb.net/leave_management

# Secure JWT signing secret (should be a random 256-bit hex or long string)
JWT_SECRET=your_jwt_secret_key_here_must_be_at_least_32_characters_long

# Google Gemini API Key (revoke and recreate if exposed!)
SPRING_AI_GOOGLE_GENAI_API_KEY=your_google_gemini_api_key_here
